APP_NAME="Rest API Skeleton"
APP_DESCRIPTION="Rest API Skeleton for Laravel"
APP_ENV=local
APP_KEY=base64:M+1a2ftn7mlpqK6glpM52syGgJrOl/Ol8ZYYNnfbuTo=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=ru
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=rest-api
DB_USERNAME=user
DB_PASSWORD=password

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

CACHE_STORE=redis
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=predis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Настройки почты
MAIL_MAILER=smtp
MAIL_HOST=smtp.ethereal.email
MAIL_PORT=587
MAIL_USERNAME=montana.jakubowski26@ethereal.email
MAIL_PASSWORD=dM5g83EgvzKkj6Shc3
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=montana.jakubowski26@ethereal.email
MAIL_FROM_NAME="Cometa Aspro Sync API"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Swagger
L5_SWAGGER_UI_PERSIST_AUTHORIZATION=true
L5_SWAGGER_DEV_HOST=http://127.0.0.1:8002/api
L5_SWAGGER_PROD_HOST=http://127.0.0.1:8002/api

# API Auth
API_AUTH_ENABLED=true
# JWT (php-open-source-saver/jwt-auth, ключ: php artisan jwt:secret)
JWT_SECRET=xxxxxxxx
# Алгоритм подписи токена
JWT_ALGO=HS256
# Время жизни access-токена (минуты). По умолчанию 60 (1 час).
JWT_TTL=60
# Окно, в течение которого токен можно обновить через refresh (минуты). По умолчанию 20160 (14 дней).
JWT_REFRESH_TTL=20160
# true = при каждом refresh обновлять iat, окно сдвигается (rolling refresh)
JWT_REFRESH_IAT=false